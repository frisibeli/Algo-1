package Queue;

class Queue<T> implements QueueInterface1<T> {

	  // Adds value to the end of the Queue.
	  // Complexity: O(1)
	  /* (non-Javadoc)
	 * @see Queue.QueueInterface1#push(T)
	 */
	@Override
	public void push(T value) {
	    // ...
	  }

	  // Returns value from the front of the Queue and removes it.
	  // Complexity: O(1)
	  /* (non-Javadoc)
	 * @see Queue.QueueInterface1#pop()
	 */
	@Override
	public T pop() {
	    // ...
	  }

	  // Returns value from the front of the Queue without removing it.
	  // Complexity: O(1)
	  /* (non-Javadoc)
	 * @see Queue.QueueInterface1#peek()
	 */
	@Override
	public T peek() {
	    // ...
	  }

	  // Returns the number of elements in the Queue.
	  // Complexity: O(1)
	  /* (non-Javadoc)
	 * @see Queue.QueueInterface1#size()
	 */
	@Override
	public int size() {
	    // ...
	  }
	}